#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer
 * @num: the int to check
 * Return: the absolute value of int
 */
int _abs(int num)
{
	return (abs(num));
}
